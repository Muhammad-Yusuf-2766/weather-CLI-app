const getArguments = require("./helpers/arguments");

const startCLI = () => {
    const args = getArguments(process.argv);
    console.log(args);

    if(args.h) {
        // help
    }

    if(args.h) {
        //help
    }
    
    if(args.t) {
        //save token

        // result
    }
};       

startCLI();
